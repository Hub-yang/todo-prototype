# 进度与待办

最后更新：2026-09-17。

## 一期：已完成

13 个教学场景全部上线，双主题走查通过。分两个计划实施，原文归档在
[`archive/`](archive/)：计划一（15 个任务，核心引擎与画布）、计划二（14 个任务，场景内容全量填充）。

| 组 | 场景 |
|---|---|
| A 基石 | a1 对象字面量的隐式原型 · a2 `new` 到底做了什么 · a3 `prototype` 与 `__proto__` 正面对决 |
| B 查找与继承 | b1 属性查找逐跳 · b2 链的终点 null · b3 屏蔽（写操作不上溯）· b4 `Object.create` 与无原型对象 |
| C 语法糖 | c1 class 展开成 ES5 · c2 `extends` 的双链 · c3 `instanceof` 原理 |
| D 全景与陷阱 | d1 鸡生蛋的环 · d2 内置对象全景图 · d3 原型污染传播 |

配套能力：探索式高亮（hover 点亮整条链）、步进播放与键盘控制、可折叠代码/讲解浮层、
`aurora` / `neon` 双主题、深链分享 `#/s/<id>?step=&theme=`、`paper` 主题离屏导出 PNG、
d2 的节点搜索与可见性裁剪。

当前基线：**34 个测试文件、316 条断言全绿**，`pnpm lint` 与 `pnpm build` 退出码均为 0。

## 待办

### 1. 导出 PNG 的落盘确认（需要本人操作）

代码路径已验证跑通：按钮状态从「导出中…」正常恢复，主题 `neon` → `paper` → `neon` 正确还原，
画布无异常。但**插件驱动的标签页会拦截页面发起的下载**，文件没有落盘。
需要在普通标签页里本人点一次导出，确认文件确实生成且图像正确。

### 2. 边标签的横向槽位分配

d1 / c3 / d2 三处仍有「标签互相压」共 3308（归一化面积），成因是同一条行间空隙里挤了三个以上标签，
而当前只按边的 kind 分两层。彻底解决要在 `ProtoCanvas` 里为同一条空隙的标签分配横向槽位，
属于新增机制而非参数调整。

当前观感：标签文字有部分重叠，但**不遮挡任何节点内容**——这是上一轮优化刻意做的取舍，
三版对照数据见 [`reviews/2026-09-11-一期走查.md`](reviews/2026-09-11-一期走查.md)。动这块之前必须读那份数据。

## 下一步

二期（代码沙盒）与三期（动手搭建练习）各自独立立项，届时按
brainstorming → writing-plans 的流程重走一遍，产物写进 `.docs/specs/` 与 `.docs/plans/`。
两期的范围边界见 [`decisions.md`](decisions.md#分期边界)。
